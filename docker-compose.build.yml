# 源码开发/本地构建覆盖文件。
# 在线升级只支持 docker-compose.yml 中的官方发布镜像部署；本地构建实例请重新 build。
services:
  nowen-note:
    build:
      context: .
      dockerfile: Dockerfile
    image: nowen-note:local
    environment:
      - NOWEN_DEPLOYMENT_TYPE=docker-compose-build
      - NOWEN_UPDATER_TOKEN=
